apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: wp-labs
  labels:
    app: api
spec:
  type: ClusterIP
  ports:
    - port: 4200
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: api
